[project]
name = "vm-app"
version = "0.1.0"
description = "Vaca Muerta dashboard built with Taipy."
requires-python = ">=3.12,<3.13"
dependencies = [
    "pandas",
    "taipy",
]

[tool.uv]
package = false
